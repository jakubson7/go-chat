import React from "react"
import styled from "styled-components"
import { HeaderText } from "../ui/text"

const Container = styled.main`

`

const HomePage = () => (
  <Container>
    <HeaderText>this is homepage</HeaderText>
  </Container>
)

export default HomePage
